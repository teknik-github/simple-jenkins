pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'build'
            }
        }
        stage('Test') {
            steps {
                echo'testing'
            }
        }
       stage('Deploy') {
        steps {
               sh 'python3 main.py'
            }
       }
    }
}
