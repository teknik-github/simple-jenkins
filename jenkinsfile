pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'apt install python3'
            }
        }
        stage('Test') {
            steps {
                sh 'python3 -v'
            }
        }
       stage('Deploy') {
        steps {
               sh 'python main.py'
            }
       }
    }
}
