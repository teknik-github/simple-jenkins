pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'sudo apt-get install python3'
            }
        }
        stage('Test') {
            steps {
                sh 'python3 -v'
            }
        }
       stage('Deploy') {
        steps {
               sh 'python main.py'
            }
       }
    }
}
