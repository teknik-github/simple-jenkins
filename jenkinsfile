pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'uild'
            }
        }
        stage('Test') {
            steps {
                echo'testing'
            }
        }
       stage('Deploy') {
        steps {
               sh 'python main.py'
            }
       }
    }
}
